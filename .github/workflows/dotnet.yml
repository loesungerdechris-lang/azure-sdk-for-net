name: .NET legacy manual

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow intentionally disabled for automatic SENTINEL CI
        run: echo "Legacy .NET workflow is manual-only in this repurposed SENTINEL monitor repository."

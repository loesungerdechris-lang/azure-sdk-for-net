# SENTINEL Monitor Repurpose Plan

This branch adds the Go-first SENTINEL monitor under `sentinel-monitor-go/`.

Current repository: `loesungerdechris-lang/azure-sdk-for-net`

Intended public name: `sentinel-monitor-go`

The existing repository contents are not removed in this branch. The monitor is isolated in its own module so the first PR is reversible and reviewable.
